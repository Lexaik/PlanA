@model IEnumerable<PlanA.ViewModels.LoadingViewModel>

@{
    ViewData["Title"] = "Загрузка производства";
}

<div class="text-center">
    <table class="table">
        <tr><th>Название</th>
            <th>Единицы</th>
        </tr>
        @foreach (var order in Model) {
            <tr>
                <td>@order.Name</td>
                <td>111</td>
            </tr>
        }
    </table>
</div>